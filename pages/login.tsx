import {FormEvent,useState} from "react";import {useRouter} from "next/router";import {tenantAuth,tenantAuthConfigured} from "../lib/tenantAuth"
export default function TenantLogin(){const router=useRouter();const [mode,setMode]=useState<"login"|"activate">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const gmail=email.trim().toLowerCase();async function submit(e:FormEvent){e.preventDefault();if(!tenantAuth)return;setBusy(true);setError("");try{if(!gmail.endsWith("@gmail.com"))throw new Error("Use the Gmail address registered by your property manager.");const result=mode==="login"?await tenantAuth.auth.signInWithPassword({email:gmail,password}):await tenantAuth.auth.signUp({email:gmail,password,options:{emailRedirectTo:`${window.location.origin}/tenant`}});if(result.error)throw result.error;if(mode==="activate"&&!result.data.session){setError("Check Gmail and open the confirmation email to finish activation.");return}await router.push("/tenant")}catch(c){setError(c instanceof Error?c.message:"Authentication failed")}finally{setBusy(false)}}async function google(){if(!tenantAuth)return;await tenantAuth.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${window.location.origin}/tenant`,queryParams:{prompt:"select_account"}}})}return <main className="grid min-h-screen place-items-center bg-[#f4f7f6] p-5"><section className="w-full max-w-md rounded-2xl border border-[#dce5e2] bg-white p-7 shadow-sm"><p className="text-sm font-bold text-[#1f6f5b]">Rentomatic tenant portal</p><h1 className="mt-2 text-3xl font-bold">{mode==="login"?"Tenant login":"Activate your account"}</h1><p className="mt-2 text-sm text-[#61716c]">Use the Gmail address registered by your property manager.</p>{!tenantAuthConfigured&&<p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">Tenant authentication is awaiting its Supabase public key.</p>}{error&&<p className="mt-4 rounded-xl bg-blue-50 p-3 text-sm text-blue-800">{error}</p>}<button onClick={google} disabled={!tenantAuthConfigured} className="mt-5 w-full rounded-xl border px-4 py-3 font-semibold disabled:opacity-50">Continue with Google</button><div className="my-5 flex items-center gap-3 text-xs text-[#788681]"><span className="h-px flex-1 bg-[#dce5e2]"/>OR<span className="h-px flex-1 bg-[#dce5e2]"/></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-semibold">Gmail address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="tenant@gmail.com" className="mt-1 w-full rounded-xl border px-4 py-3" required/></label><label className="block text-sm font-semibold">Password<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 w-full rounded-xl border px-4 py-3" required/></label><button disabled={busy||!tenantAuthConfigured} className="w-full rounded-xl bg-[#1f6f5b] px-4 py-3 font-semibold text-white disabled:opacity-50">{busy?"Please wait…":mode==="login"?"Sign in":"Create tenant account"}</button></form><button onClick={()=>{setError("");setMode(mode==="login"?"activate":"login")}} className="mt-5 w-full text-sm font-semibold text-[#1f6f5b]">{mode==="login"?"First time? Activate account":"Back to sign in"}</button></section></main>}
